import { Helmet } from 'react-helmet-async';
import type { JSX } from 'react';

/** Placeholder — wire load-by-id + `AdminProductV2Form` mode="read" here. */
export default function AdminReadProduct(): JSX.Element {
  return (
    <>
      <Helmet>
        <title>View product — Admin</title>
      </Helmet>
      <p className="text-body-regular text-muted-foreground">Admin read product — coming soon.</p>
    </>
  );
}
