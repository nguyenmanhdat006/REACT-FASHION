import { Helmet } from 'react-helmet-async';
import type { JSX } from 'react';

/** Placeholder — wire load-by-id + `AdminProductV2Form` mode="update" here. */
export default function AdminUpdateProduct(): JSX.Element {
  return (
    <>
      <Helmet>
        <title>Edit product — Admin</title>
      </Helmet>
      <p className="text-body-regular text-muted-foreground">Admin update product — coming soon.</p>
    </>
  );
}
